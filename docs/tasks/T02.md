---
id: T02
title: "Contrato global de CLI"
status: done
phase: 0
depends: [T01]
manual: false
---

# T02 — Contrato global de CLI

## Done when

- [x] Flag global `--json` **antes** do subcomando
- [x] Exit codes: 0 ok, 1 domínio, 2 sistema, 64 usage
- [x] Erros em stderr; JSON stdout documentado em docs/behavior.md

## Notes

Espelhar ordem de flags do CLI `apple` em ~/nix/clis/apple.

Closed after review: `MacverbsApp.runCatching` + injectable stdio prove exit 1/2 and stderr; JSON emit tested on captured handles.

## Spec / refs

- `AGENTS.md`
- `docs/ROADMAP.md`
- `docs/behavior.md`
- Behavior oracle: `~/nix/clis/apple` (env `MACVERBS_APPLE_SPEC`)

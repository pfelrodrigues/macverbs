---
id: T02
title: "Contrato global de CLI"
status: pending
phase: 0
depends: [T01]
manual: false
---

# T02 — Contrato global de CLI

## Done when

- [ ] Flag global `--json` **antes** do subcomando
- [ ] Exit codes: 0 ok, 1 domínio, 2 sistema, 64 usage
- [ ] Erros em stderr; JSON stdout documentado em docs/behavior.md

## Notes

Espelhar ordem de flags do CLI `apple` em ~/nix/clis/apple.

## Spec / refs

- `AGENTS.md`
- `docs/ROADMAP.md`
- `docs/behavior.md`
- Behavior oracle: `~/nix/clis/apple` (env `MACVERBS_APPLE_SPEC`)

## Workflow

```text
# from repo root
mise run next          # should print this id when it is the first pending
# or explicit:
# workflow implement-task with args.task=T02
```

When this task is finished: set `status: done` in the frontmatter and check the boxes above.

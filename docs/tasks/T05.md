---
id: T05
title: "Config / paths"
status: pending
phase: 0
depends: [T03]
manual: false
---

# T05 — Config / paths

## Done when

- [ ] Load de ~/.config/macverbs/calendars.json (ou MACVERBS_CONFIG_DIR)
- [ ] Exemplo calendars.example.json no repo
- [ ] Sem nomes Vert/PYO hardcoded

## Notes

Config ausente = defaults vazios, sem crash.

## Spec / refs

- `AGENTS.md`
- `docs/ROADMAP.md`
- `docs/behavior.md`
- Behavior oracle: `~/nix/clis/apple` (env `MACVERBS_APPLE_SPEC`)

## Workflow

```text
# from repo root
mise run next          # should print this id when it is the first pending
# or explicit:
# workflow implement-task with args.task=T05
```

When this task is finished: set `status: done` in the frontmatter and check the boxes above.

---
id: T01
title: "Scaffold SPM + binário"
status: done
phase: 0
depends: []
manual: false
---

# T01 — Scaffold SPM + binário

## Done when

- [x] Package.swift com executable product `macverbs`
- [x] `swift build` e `swift test` passam (teste mínimo ok)
- [x] `macverbs --version` e `--help` funcionam via `mise run run -- --version` ou `swift run`

## Notes

Criar layout Sources/macverbs, Tests/macverbsTests, ArgumentParser dependency.

Closed after workflow verify gaps: behavior.md status for --version/--help; mise run bare no longer injects empty argv (`scripts/macverbs_run.sh`).

## Spec / refs

- `AGENTS.md`
- `docs/ROADMAP.md`
- `docs/behavior.md`
- Behavior oracle: `~/nix/clis/apple` (env `MACVERBS_APPLE_SPEC`)

## Workflow

```text
# from repo root
mise run next          # should print this id when it is the first pending
# or explicit:
# workflow implement-task with args.task=T01
```

When this task is finished: set `status: done` in the frontmatter and check the boxes above.

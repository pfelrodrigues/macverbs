---
id: T03
title: "Seams + doctor stub"
status: done
phase: 0
depends: [T02]
manual: false
---

# T03 — Seams + doctor stub

## Done when

- [x] Protocols EventStoreClient e ScriptRunner injetáveis
- [x] `macverbs doctor` roda sem TCC e reporta o que falta
- [x] Unit tests com mocks

## Notes

Nenhuma chamada real a EventKit/osascript neste task.

## Spec / refs

- `AGENTS.md`
- `docs/ROADMAP.md`
- `docs/behavior.md`
- Behavior oracle: `~/nix/clis/apple` (env `MACVERBS_APPLE_SPEC`)

## Workflow

```text
# from repo root
mise run next          # should print this id when it is the first pending
# or explicit:
# workflow implement-task with args.task=T03
```

When this task is finished: set `status: done` in the frontmatter and check the boxes above.

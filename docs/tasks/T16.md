---
id: T16
title: "mail archive + delete (efeito verificado)"
status: pending
phase: 2
depends: [T15]
manual: false
---

# T16 — mail archive + delete (efeito verificado)

## Done when

- [ ] Move + recontagem moved/requested
- [ ] Gmail archive → unsupported (não mentir)
- [ ] --account obrigatório

## Notes

Ver regra verificar-efeito-real-cli do usuário.

## Spec / refs

- `AGENTS.md`
- `docs/ROADMAP.md`
- `docs/behavior.md`
- Behavior oracle: `~/nix/clis/apple` (env `MACVERBS_APPLE_SPEC`)

## Workflow

```text
# from repo root
mise run next          # should print this id when it is the first pending
# or explicit:
# workflow implement-task with args.task=T16
```

When this task is finished: set `status: done` in the frontmatter and check the boxes above.
